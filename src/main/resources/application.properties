spring.application.name=usertemplate

# application-dev.properties is secret
spring.profiles.active=dev

# server port 8080
server.port=8080

# logging level
logging.level.root=WARN
logging.level.com.example.usertemplate=DEBUG
